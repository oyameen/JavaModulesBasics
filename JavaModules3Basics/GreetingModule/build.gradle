plugins {
    id 'java'
}

group = 'com.oyameen'
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    implementation(project(':HelloModule'))
    implementation(project(':HiModule'))
}

test {
    useJUnitPlatform()
}